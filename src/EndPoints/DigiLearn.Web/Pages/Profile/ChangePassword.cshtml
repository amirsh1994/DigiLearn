@page
@model DigiLearn.Web.Pages.Profile.ChangePasswordModel
@{
    ViewData["title"] = "  ویرایش اطلاعات پروفایل";
    Layout = "Shared/_ProfileLayout";
}

                <div class="row ">
                    <div class="col-12 info shadow pl-0 pr-0">
                        <p class="title">
                            <i class="fa fa-address-card-o"></i>
                           @ViewData["title"]
                        </p>
                        <div class="info-body p-2">
                            <form class="row" method="post">

                                <div class="col-12">
                                    @Html.EditorFor(x => x.CurrentPassword)
                                </div>

                                <div class="col-12 col-lg-6 col-md-6 mb-3">
                                    @Html.EditorFor(x => x.NewPassword)
                                </div>

                                <div class="col-12 col-lg-6 col-md-6 mb-3">
                                    @Html.EditorFor(x => x.ConfirmedNewPassword)
                                </div>
                                <div class="col-12">
                                    <button class="btn  btn-save  mt-3"> تغییر کلمه عبور</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
